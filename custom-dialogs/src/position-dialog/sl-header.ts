const slHeaderElement = document.createElement('div');
slHeaderElement.className = 'custom-position-dialog__sl-header';
slHeaderElement.textContent = 'Stop Loss';

export const slHeader = slHeaderElement;
