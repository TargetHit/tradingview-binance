const cancelButtonElement = document.createElement('button');
cancelButtonElement.className = 'custom-order-dialog__cancel-button';
cancelButtonElement.textContent = 'Cancel';

export const cancelButton = cancelButtonElement;
